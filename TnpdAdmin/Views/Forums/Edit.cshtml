@using TnpdModels
@model TnpdModels.Forums

@{
    ViewBag.Title = "編輯";
}
<div class="tabbable">
    <!-- Only required for left/right tabs -->
    <ul class="nav nav-tabs" id="myTab">
        <li class="active"><a href="#tab1" data-toggle="tab">基本資料</a></li>
        <li><a href="#tab2" data-toggle="tab">附加檔案</a></li>
        <li><a href="#tab3" data-toggle="tab">附加連結</a></li>
    </ul>
    <div class="tab-content">
        <div class="tab-pane active" id="tab1">
            <h2 class="page-title"><i class="fa fa-pencil-square-o"></i>編輯</h2>

            @using (Html.BeginForm())
            {

                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true)

                <fieldset>
                    <legend>Forums</legend>

                    @Html.HiddenFor(model => model.Id)

                    <div class="editor-label">
                        @Html.LabelFor(model => model.CategoryId)
                    </div>
                    <div class="editor-field">
                        @Html.DropDownList("CategoryId")
                        @Html.ValidationMessageFor(model => model.CategoryId)

                    </div>

                    <div class="editor-label">
                        @Html.LabelFor(model => model.Title)
                    </div>
                    <div class="editor-field">
                        @Html.TextBoxFor(model => model.Title, new { placeholder = ModelsAttributeHelper<TnpdModels.Forums>.GetDisplayName("Title") })

                    </div>

                    <div class="editor-label">
                        @Html.LabelFor(model => model.StartDate)
                    </div>
                    <div class="editor-field">
                        @Html.TextBoxFor(model => model.StartDate, "{0:d}", new { @class = "datepicker", target = "" })
                        @Html.ValidationMessageFor(model => model.StartDate)

                    </div>

                    <div class="editor-label">
                        @Html.LabelFor(model => model.Article)
                    </div>
                    <div class="editor-field">
                        @Html.TextAreaFor(model => model.Article, new { @class = "ckeditor" })
                        @Html.ValidationMessageFor(model => model.Article)

                    </div>

                    @Html.HiddenFor(model => model.Poster)
                    @Html.HiddenFor(model => model.InitDate)
                    @Html.HiddenFor(model => model.UpdateDate)
                    <p>
                        <input type="submit" value="更新" class="btn-button" />
                    </p>
                </fieldset>
            }
        </div>
        <div class="tab-pane" id="tab2">
            <p>
                <a id="colorlink" onclick="$.fn.colorbox({ width: '700', height: '600', iframe: true, href: '@Url.Action("Edit", "ForumsFiles", new { id=0, ForumsId = Model.Id })' })" href="javascript:return false;">新增檔案</a>                
            </p>
            <table class="table" id="FileTable">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.ForumsFileses.FirstOrDefault().Subject)
                    </th>
                    

                    <th>
                        @Html.DisplayNameFor(model => model.ForumsFileses.FirstOrDefault().ListNum)
                    </th>
                    <th>
                        新排序
                    </th>
                    <th></th>
                </tr>
                <tbody>
                    @*注意這邊,要指定排序*@
                    @foreach (var item in Model.ForumsFileses.OrderBy(m => m.ListNum))
                    {
                        <tr>
                            
                            <td>
                                @if (!string.IsNullOrEmpty(item.UpFile))
                                {
                                    <a href="@item.UpFile">@item.Subject</a>
                                }
                            </td>

                            <td>
                                @Html.DisplayFor(modelItem => item.ListNum)
                            </td>
                            <td>

                                @Html.TextBoxFor(modelItem => item.ListNum, new { @class = "position" })
                            </td>
                            <td>

                                <input type="button" onclick="$.colorbox({ width: '80%', height: '80%', iframe: true, href: '@Url.Action("Edit", "ForumsFiles", new { id = item.Id })'})" value="編輯" class="btn btn-primary" />
                                
                                @using (Html.BeginForm("Delete", "ForumsFiles"))
                                {
                                    @Html.AntiForgeryToken()

                                    <input class="btn btn-primary"  type="submit" value="刪除" onclick="javascript: if (!window.confirm('你確定要刪除嗎?')) window.event.returnValue = false;" />

                                    @*這邊也有改*@
                                    @Html.Hidden("fileid", item.Id, new { @class = "fid" })
                                }

                            </td>
                        </tr>
                    }
                </tbody>
            </table>

            @using (Html.BeginForm("Sort", "ForumsFiles"))
            {
                <div>
                    <input type="hidden" name="Id" value="@Model.Id" />
                    <input type="hidden" id="fileSortData" name="SortData" />
                    <input type="submit" value="更新排序" class="btn-button" />

                </div>
            }
        </div>
        <div class="tab-pane" id="tab3">
            <p>
                <a id="colorlink" onclick="$.fn.colorbox({ width: '700', height: '600', iframe: true, href: '@Url.Action("Edit", "ForumsLink", new { id=0, ForumsId = Model.Id })' })" href="javascript:return false;">新增連結</a>
            </p>
            <table class="table" id="LinkTable">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.ForumsLinks.FirstOrDefault().Subject)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ForumsLinks.FirstOrDefault().LinkUrl)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ForumsLinks.FirstOrDefault().Target)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ForumsLinks.FirstOrDefault().ListNum)
                    </th>
                    <th>
                        新排序
                    </th>
                    <th></th>
                </tr>
                <tbody>
                    @*注意這邊,要指定排序*@
                    @foreach (var item in Model.ForumsLinks.OrderBy(m => m.ListNum))
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Subject)
                            </td>
                            <td>
                                @if (!string.IsNullOrEmpty(item.LinkUrl))
                                {
                                    <a href="@item.LinkUrl">@item.LinkUrl</a>
                                }
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Target)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ListNum)
                            </td>
                            <td>

                                @Html.TextBoxFor(modelItem => item.ListNum, new { @class = "position" })
                            </td>
                            <td>

                                <input type="button" onclick="$.colorbox({ width: '80%', height: '80%', iframe: true, href: '@Url.Action("Edit", "ForumsLink", new { id = item.Id })'})" value="編輯" class="btn btn-primary" />
                               
                                @using (Html.BeginForm("Delete", "ForumsLink"))
                                {
                                    @Html.AntiForgeryToken()
                                    <input class="btn btn-primary"  type="submit" value="刪除" onclick="javascript: if (!window.confirm('你確定要刪除嗎?')) window.event.returnValue = false;" />
                                    @*這邊也有改*@
                                    @Html.Hidden("linkid", item.Id, new { @class = "fid" })
                                }

                            </td>
                        </tr>
                    }
                </tbody>
            </table>

            @using (Html.BeginForm("Sort", "ForumsLink"))
            {
                <div>
                    <input type="hidden" name="Id" value="@Model.Id" />
                    <input type="hidden" id="linkSortData" name="SortData" />
                    <input type="submit" value="更新排序" class="btn-button" />

                </div>
            }

        </div>
    </div>
</div>

<div>
    @Html.ActionLink("回列表", "Index", new { Page = -1 })
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script src="@Url.Content("~/Scripts/jquery.tablednd.0.5.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/jquery.colorbox-min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/ckeditor/ckeditor.js")" type="text/javascript"></script>
    <link href="@Url.Content("~/Content/colorbox.css")" rel="stylesheet" type="text/css" />
    @Scripts.Render("~/bundles/jqueryui")
    @Styles.Render("~/Content/themes/base/css")
    <script>
        $(document).ready(function () {
            $(".datepicker").datepicker();
        });
        $('#myTab a:eq(@ViewBag.type)').tab('show');
        $("#FileTable tbody").tableDnD({
            onDragClass: "showDragHandle",
            onDrop: function (table, row) {
                var rows = table.rows;
                var map = "";
                for (var i = 0; i < rows.length; i++) {
                    $(rows[i]).find(".position").val(i + 1);
                    map += $(rows[i]).find(".fid").val() + ":" + (i + 1) + ",";
                }
                $("#fileSortData").val(map);
            }
        });
        $("#LinkTable tbody").tableDnD({
            onDragClass: "showDragHandle",
            onDrop: function (table, row) {
                var rows = table.rows;
                var map = "";
                for (var i = 0; i < rows.length; i++) {
                    $(rows[i]).find(".position").val(i + 1);
                    map += $(rows[i]).find(".fid").val() + ":" + (i + 1) + ",";
                }
                $("#linkSortData").val(map);
            }
        });
    </script>
}
